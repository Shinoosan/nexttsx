import { dynamic, runtime } from '../config';
import { ProfileContent } from '@/components/views/profile-content';

export { dynamic, runtime };

export default function ProfilePage() {
  return <ProfileContent />;
}
